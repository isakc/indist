<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
"-//mybatis.org/DTD/Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cart">
	<select id="selectCartList" parameterType="int" resultType="Cart">
		SELECT c.no, c.userno, c.albumno, c.count, c.regdate, a.price, a.coverimg, a.albumtitle, a.explain
		FROM cart c, albums a
		WHERE c.userNo = #{userNo}
		AND c.albumno = a.no
	</select>
</mapper>
